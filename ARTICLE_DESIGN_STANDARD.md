# استاندارد ظاهری مقاله‌های ThinkFlow

این سند مرجع الزامی طراحی و انتشار همه مقاله‌های ThinkFlow است. پیش از ساخت یا ویرایش هر صفحه در مسیر `/blog/...` باید این فایل همراه با `CONTENT_STANDARD.md` خوانده و اجرا شود. هدف، یک تجربه خواندن یکدست در تمام مقاله‌هاست؛ تفاوت موضوع مقاله نباید باعث تغییر ناخواسته در جای فهرست، عرض متن، ترازبندی تیتر یا رفتار موبایل شود.

## 1. اصول ثابت

- زبان صفحه فارسی، `lang="fa"` و جهت کلی `dir="rtl"` است.
- هدر سایت، لوگو، ناوبری و فوتر باید با دیگر صفحات عمومی ThinkFlow یکسان باشد.
- در دسکتاپ، عنوان، لید و اطلاعات انتشار وسط‌چین هستند.
- در دسکتاپ، فهرست مطالب در ستون راست و متن مقاله در ستون چپ قرار می‌گیرد.
- در موبایل، فهرست پیش از متن مقاله نمایش داده می‌شود و عنوان برای خوانایی راست‌چین است.
- هر مقاله فقط یک `h1` دارد و متن اصلی بدون JavaScript نیز قابل خواندن است.
- کارت‌های تو در تو، تزئینات نامرتبط و تغییر خودسرانه پالت رنگ مجاز نیست.

## 2. توکن‌های پایه

مقاله‌ها باید از این توکن‌ها یا معادل دقیق آن‌ها در فایل مشترک استفاده کنند:

```css
:root {
  --bg: #070a12;
  --surface: #101624;
  --surface-2: #151d2e;
  --accent: #5eead4;
  --text: #e2e8f0;
  --muted: #a8b3c4;
  --border: rgba(148, 163, 184, 0.22);
  --radius: 8px;
}
```

فونت عادی باید از فایل Regular و وزن‌های `700` تا `900` از فایل Bold بارگذاری شوند. فاصله حروف صفر است و اندازه فونت نباید با عرض viewport به‌صورت پیوسته مقیاس شود.

## 3. اسکلت اجباری DOM

ترتیب عناصر زیر بخشی از طراحی است. در صفحه RTL، قرار گرفتن `aside` پیش از `.prose` باعث می‌شود فهرست در ستون راست باشد.

```html
<main>
  <nav class="breadcrumb" aria-label="مسیر صفحه">...</nav>
  <article>
    <header class="article-header">
      <p class="kicker">دسته‌بندی</p>
      <h1>عنوان مقاله</h1>
      <p class="dek">خلاصه مقاله</p>
      <div class="article-meta">نویسنده، تاریخ‌ها و زمان مطالعه</div>
    </header>

    <figure class="hero-figure">...</figure>

    <div class="article-layout">
      <aside class="toc" aria-label="فهرست مطالب">
        <strong>در این مقاله</strong>
        <nav>...</nav>
      </aside>
      <div class="prose">...</div>
    </div>
  </article>
</main>
```

نام کلاس‌های این اسکلت نباید در یک مقاله جدید تغییر کند. اجزای اختصاصی مانند جدول، مثال، پرامپت و هشدار باید داخل `.prose` قرار گیرند.

## 4. ابعاد و چیدمان دسکتاپ

- عرض `main`: حداکثر `1180px` با حاشیه افقی `18px`.
- فاصله عمودی `main`: بالا `32px` و پایین `72px`.
- عرض هدر مقاله و تصویر اصلی: حداکثر `940px` و وسط صفحه.
- عنوان: `42px` با `line-height: 1.55` و `text-align: center`.
- لید: حداکثر `820px`، اندازه `17px` و وسط‌چین.
- اطلاعات مقاله: `display:flex`، امکان شکستن خط و `justify-content:center`.
- فاصله پایین تصویر اصلی: `42px`.
- شبکه محتوا: `grid-template-columns: 250px minmax(0, 1fr)` با فاصله `38px`.
- فهرست: `position:sticky` و `top:84px`.
- متن مقاله: `15px` با `line-height:2.15`.
- تیترهای `h2`: اندازه `26px`، فاصله بالا `50px` و خط جداکننده پایین.

## 5. واکنش‌گرایی

در عرض `900px` و کمتر:

- `.article-layout` تک‌ستونه می‌شود.
- فهرست از حالت sticky خارج می‌شود و به‌دلیل ترتیب DOM پیش از متن می‌ماند.
- لینک‌های فهرست در دو ستون نمایش داده می‌شوند.
- عنوان حداکثر `38px` است.

در عرض `660px` و کمتر:

- حاشیه افقی صفحه `12px` است.
- هدر مقاله و عنوان راست‌چین می‌شوند.
- عنوان `30px` و لید `14px` است.
- اطلاعات مقاله از ابتدای محور چیده می‌شود.
- فهرست تک‌ستونه است.
- جدول‌ها داخل `.table-wrap` اسکرول افقی کنترل‌شده دارند؛ خود صفحه نباید اسکرول افقی داشته باشد.
- دکمه‌های CTA تمام‌عرض می‌شوند.

## 6. تصویر و رسانه

- تصویر اصلی روی دامنه ThinkFlow میزبانی و ترجیحاً WebP بهینه باشد.
- `width` و `height` واقعی، متن جایگزین دقیق و `figcaption` الزامی است.
- تصویر اصلی `fetchpriority="high"` دارد؛ تصاویر پایین مقاله `loading="lazy"` دارند.
- تصویر اصلی در قاب `16/9` با `object-fit:cover` نمایش داده می‌شود.
- تصویر شکسته، منبع موقت یا رسانه‌ای که بدون دسترسی خارجی سفید می‌ماند نباید منتشر شود.

## 7. اجزای داخل مقاله

- جدول‌ها فقط داخل `.table-wrap` قرار می‌گیرند.
- هشدار و نکته با `.callout` نمایش داده می‌شوند؛ هشدار جدی می‌تواند کلاس مکمل `.warning` داشته باشد.
- نمونه پرامپت با `.prompt-box` و جهت مناسب متن نمایش داده می‌شود.
- FAQ فقط زمانی استفاده می‌شود که سؤال و پاسخ در صفحه قابل مشاهده و با JSON-LD هماهنگ باشد.
- CTA پایانی با `.article-end` و حداکثر سه اقدام مرتبط ساخته می‌شود.
- اجزای جدید باید از همان توکن‌ها استفاده کنند و نباید یک سیستم بصری جدا بسازند.

## 8. کنترل اجباری پیش از انتشار

هر مقاله باید پیش از استقرار در این دو viewport بررسی شود:

1. دسکتاپ: `1440 × 900`
2. موبایل: `390 × 844`

چک‌لیست انتشار:

- عنوان در دسکتاپ وسط‌چین است.
- فهرست در دسکتاپ از نظر مختصات در سمت راست متن قرار دارد.
- در موبایل، فهرست پیش از متن و عنوان راست‌چین است.
- هیچ متن، جدول، دکمه یا تصویری از عرض صفحه بیرون نمی‌زند.
- تصویر اصلی و همه منابع محلی با وضعیت موفق بارگذاری می‌شوند.
- فقط یک `h1` وجود دارد و ترتیب `h2` و `h3` صحیح است.
- لینک‌های فهرست به شناسه‌های موجود می‌رسند.
- JSON-LD معتبر است و اطلاعات آن با متن قابل مشاهده تطابق دارد.
- صفحه با آخرین مقاله مرجع هماهنگ مقایسه بصری شده است.

اگر یک تغییر طراحی قرار است در همه مقاله‌ها اعمال شود، ابتدا این سند و مقاله مرجع به‌روزرسانی شوند و سپس تغییر به همه صفحات منتقل شود. اصلاح موردی یک مقاله که باعث تفاوت ظاهری با بقیه شود مجاز نیست.
